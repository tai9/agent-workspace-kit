# Conversion audit — <YYYY-MM-DD>

Window: <from → to, plus comparison window>. Volumes: <actives, payers —
absolute>. Author: conversion-audit. Status: draft | delivered.

## 1. The one thing

The single decided fix. What, why this one first (expected value ÷
effort, in users/week or revenue/month), how we'll know it worked (the
event or query, the threshold, by when).

## 2. Value chain — where users are actually lost

| Link | Definition used | Users in → out (window) | vs prior | Confidence |
| --- | --- | --- | --- | --- |
| install → account | | | | |
| account → activation | | | | |
| activation → habit | | | | |
| habit → limit felt | | | | |
| limit → paywall seen | | | | |
| paywall → purchase | | | | |
| purchase → renewal/30-day survival | | | | |

Unmeasurable links, named, each with the cheapest instrumentation that
would light it up. The weakest link, split by at least one segment, and
the one-sentence conclusion: the earliest link losing the most users
per week.

## 3. Findings

Ranked. Per finding:

### F<n> — <short imperative title>
- **Claim:**
- **Evidence:** (path:line, table row + date read, or the query — never
  a doc or memory)
- **Users/week or revenue/month affected:**
- **Confidence:** verified | hypothesis (N=…)
- **Fix shape & effort:**

## 4. Content / supply

Demand vs supply per unit; the diagnosis per underperformer
(starvation / cliff / wrong band / discovery) and the sized
recommendation in the pipeline's real units.

## 5. Bugs, ranked by money

Chain position × users/week × silence. The money path (purchase,
restore, renewal, entitlement, grants) covered explicitly even when
clean.

## 6. What I could not determine

Each blind spot, why, and the cheapest way to remove it. This section
is a deliverable, not an apology.

## 7. Deliberately not recommended

The obvious-looking ideas rejected, each with the reason — the section
that makes the rest credible.

## 8. Sequence

The ordered next steps after the one thing, each with its deciding
metric and the observation that would reorder it.
