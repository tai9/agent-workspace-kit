# Unreleased — {{NAME}} app

Items merged into `{{TRUNK}}` but not yet shipped. `agent-workspace-kit release add` appends a row
here on each squash-merge; `agent-workspace-kit release cut` moves the picked rows out into a
versioned store note (`<version>.md`) or a patch note
(`<live-version>.patchN.md`). See `RELEASE_PROCESS.md`.

_Last updated: never_

| # | Channel | Type | Summary | App commit | BE commit | Coupled |
| - | ------- | ---- | ------- | ---------- | --------- | ------- |
<!-- ITEMS:START -->
<!-- ITEMS:END -->
